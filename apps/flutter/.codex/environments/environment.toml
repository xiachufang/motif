# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "flutter_motif"

[setup]
script = '''
set -eu

if command -v flutter >/dev/null 2>&1; then
  :
elif [ -x "/Users/feichao/Developer/flutter/bin/flutter" ]; then
  export PATH="/Users/feichao/Developer/flutter/bin:$PATH"
else
  echo "Flutter was not found on PATH, and /Users/feichao/Developer/flutter/bin/flutter does not exist." >&2
  exit 1
fi

ghostty_reference="${CODEX_SOURCE_TREE_PATH:-}/ghostty"

if [ -n "${CODEX_SOURCE_TREE_PATH:-}" ] && git -C "$ghostty_reference" rev-parse --git-dir >/dev/null 2>&1; then
  git submodule update --init --recursive --reference "$ghostty_reference" ghostty
else
  git submodule update --init --recursive ghostty
fi

flutter pub get

if ! command -v zig >/dev/null 2>&1; then
  echo "Warning: Zig 0.15.2 is not on PATH. Native libghostty builds and native tests may fail until Zig is installed." >&2
elif [ "$(zig version)" != "0.15.2" ]; then
  echo "Warning: Zig 0.15.2 is expected, but found $(zig version). Native libghostty builds may fail." >&2
fi

'''
