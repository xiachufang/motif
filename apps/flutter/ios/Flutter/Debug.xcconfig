#include? "Pods/Target Support Files/Pods-Runner/Pods-Runner.debug.xcconfig"
#include "Generated.xcconfig"

// Tailscale is packaged as a dynamic native asset. The iOS hook wraps the
// libtailscale c-archive into a dylib/framework and Dart loads it dynamically.
